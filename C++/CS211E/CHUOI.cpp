#include<iostream>
using namespace std;
main()
{
	char st;
	getchar(St);
	putchar(st);
	char str[50];
	gets(str);
	puts(str);
	cin.getline(st,50);
	string stt;
	getline(cin, stt);
	getline(infile,stt);  // dung cho file
}
